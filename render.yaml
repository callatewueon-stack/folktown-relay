services:
  - type: web
    name: folktown-relay
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 4 -w 1 server:app
    plan: free
