services:
  - type: web
    name: folktown-relay
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 4 --threads 4 --timeout 60 server:app
    plan: free
